package com.example.android.footballscorekeeper;

/**
 * Created by dobry on 12.03.17.
 */

public class ButtonsLock {

   /* boolean isLocked = true; //TODO: Sprawdzić co będzie po obróceniu ekranu

    // *** BUTTONS MAPPING
    // Top Buttons
    private ImageButton playButton = (ImageButton) findViewById(R.id.playButton);
    private ImageButton resetButton = (ImageButton) findViewById(R.id.resetButton);
    private ImageButton addTimeButton = (ImageButton) findViewById(R.id.addTimeButton);

    // Team A Buttons
    private Button goalForTeamAButton = (Button) findViewById(R.id.goalForTeamAButton);
    private ImageButton yellowCardAddButtonTeamA = (ImageButton) findViewById(R.id.yellow_card_add_button_team_a);
    private ImageButton redCardAddButtonTeamA = (ImageButton) findViewById(R.id.red_card_add_button_team_a);

    // Team B Buttons
    private Button goalForTeamBButton = (Button) findViewById(R.id.goalForTeamBButton);
    private ImageButton yellowCardButtonTeamB = (ImageButton) findViewById(R.id.yellow_card_add_button_team_b);
    private ImageButton redCardButtonTeamB = (ImageButton) findViewById(R.id.red_card_add_button_team_b);

    // Score Board Buttons
    private Button sentDataToScoreBoardButton = (Button) findViewById(R.id.sentToScoreBoardButton);
    private Button switchViewOfScoreBoardButton = (Button) findViewById(R.id.sentToScoreBoardButton);

    public void buttonsEnabled(Boolean status) {

        playButton.setEnabled(true);
        sentDataToScoreBoardButton.setEnabled(true);
        switchViewOfScoreBoardButton.setEnabled(true);
        resetButton.setEnabled(status);
        addTimeButton.setEnabled(status);
        goalForTeamAButton.setEnabled(status);
        yellowCardAddButtonTeamA.setEnabled(status);
        redCardAddButtonTeamA.setEnabled(status);
        goalForTeamBButton.setEnabled(status);
        yellowCardButtonTeamB.setEnabled(status);
        redCardButtonTeamB.setEnabled(status);
    }*/
}
